MATCH (n)-[r]-() DELETE n, r;
MATCH n DELETE n;

MATCH (n) RETURN n;

MATCH (m:Match)<--(p:Pick {playerId: 343289304}) RETURN m; // Матчи по playerId
MATCH (p:Pick)-->(m:Match {matchId: 1}) RETURN p; // Пики по matchId